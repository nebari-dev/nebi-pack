name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Fast chart render tests (no cluster): `helm template` + assertions in
  # tests/. Kept a separate job from `integration` so it stays green and quick
  # independent of the Nebari sandbox bootstrap.
  chart:
    name: chart render tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1

      - name: Build helm dependencies
        run: helm dependencies build

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Run chart tests
        run: uv run pytest -v

  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Full Nebari platform (NIC + ArgoCD + Keycloak + operator + gateway) so
      # the chart is exercised the way it actually runs: the operator provisions
      # the OIDC client and the gateway enforces auth.
      - uses: nebari-dev/action-nebari-sandbox@9ac369ebf87ac2ae217504dcbf824c77f70e429a # v3.0.0
        id: sandbox

      - name: Surface KUBECONFIG
        run: echo "KUBECONFIG=${{ steps.sandbox.outputs.kubeconfig }}" >> "$GITHUB_ENV"

      # Stage the chart without .git — add-software-pack commits the copied dir
      # into the gitops repo, and a nested .git would be recorded as an empty
      # submodule (gitlink) instead of the chart files.
      - name: Stage chart
        run: |
          mkdir -p /tmp/chart
          rsync -a --exclude='.git' ./ /tmp/chart/

      # Deploy this chart via GitOps and wait for the ArgoCD Application to go Healthy.
      - uses: nebari-dev/action-nebari-sandbox/add-software-pack@9ac369ebf87ac2ae217504dcbf824c77f70e429a # v3.0.0
        with:
          gitops-dir: ${{ steps.sandbox.outputs.gitops-dir }}
          app-name: nebi-pack
          chart-source: /tmp/chart
          application-manifest: ./.github/ci/nebi-pack-application.yaml

      - name: Verify deployment and operator-wired OIDC
        run: |
          # add-software-pack returns as soon as the Application reports
          # Healthy, which a freshly created app does vacuously before its
          # first sync creates any resources. Wait for the Deployment to
          # exist before asserting anything about it (rollout status with a
          # selector exits 0 on zero matches, so it is not a wait).
          for i in $(seq 1 60); do
            kubectl get deploy -n nebi -l app.kubernetes.io/name=nebari-nebi-pack \
              -o name 2>/dev/null | grep -q . && break
            echo "Waiting for nebi deployment to be created... (${i}/60)"
            sleep 5
          done
          kubectl rollout status deployment -n nebi \
            -l app.kubernetes.io/name=nebari-nebi-pack --timeout=180s

          # nebi keeps basic auth and gets the OIDC verifier wired in via the
          # operator-provisioned client (issuer derived from the hostname).
          env=$(kubectl get deploy -n nebi -l app.kubernetes.io/name=nebari-nebi-pack \
            -o jsonpath='{.items[0].spec.template.spec.containers[0].env[*].name}')
          echo "$env" | grep -q NEBI_AUTH_OIDC_ISSUER_URL \
            || { echo "OIDC env not wired"; exit 1; }

      - name: Verify the gateway redirects nebi to Keycloak
        # Reach the gateway through a port-forward to Envoy's LoadBalancer
        # Service rather than the LB IP, which is not routable from the
        # runner's network namespace.
        run: |
          svc=$(kubectl get svc -n envoy-gateway-system \
            -o jsonpath='{.items[?(@.spec.type=="LoadBalancer")].metadata.name}')
          kubectl port-forward -n envoy-gateway-system "svc/${svc}" 8443:443 &
          pf=$!
          trap 'kill $pf' EXIT
          for i in $(seq 1 30); do
            curl -sk -o /dev/null --connect-timeout 2 https://127.0.0.1:8443/ && break
            sleep 1
          done
          loc=$(curl -sk -o /dev/null -w '%{redirect_url}' \
            --resolve "nebi.nebari.local:8443:127.0.0.1" \
            https://nebi.nebari.local:8443/)
          echo "redirect: $loc"
          echo "$loc" | grep -q 'keycloak.nebari.local/realms/nebari/protocol/openid-connect/auth' \
            || { echo "nebi did not redirect to Keycloak"; exit 1; }

      - name: Cleanup
        if: always()
        run: kind delete cluster --name ${{ steps.sandbox.outputs.cluster-name }} || true
